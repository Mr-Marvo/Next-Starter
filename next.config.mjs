/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  trailingSlash: true,
  images: {
    unoptimized: true,
    loader: "default",
    path: "",
  },
};

export default nextConfig;
